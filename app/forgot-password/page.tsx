import { AuthScreen } from "@/components/auth-screen";
export const metadata = { title: "Reset your password — Hemova" };
export default function ForgotPasswordPage() { return <AuthScreen mode="forgot" />; }
