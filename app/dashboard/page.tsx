import { DonorDashboard } from "@/components/donor-dashboard";

export const metadata = { title: "Your dashboard — Hemova" };

export default function DashboardPage() {
  return <DonorDashboard />;
}
