import { RequestsExperience } from "@/components/requests-experience";

export const metadata = { title: "Verified blood requests — Hemova" };

export default function RequestsPage() { return <RequestsExperience />; }
