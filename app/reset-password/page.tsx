import { AuthScreen } from "@/components/auth-screen";
export const metadata = { title: "Choose a new password — Hemova" };
export default function ResetPasswordPage() { return <AuthScreen mode="reset" />; }
